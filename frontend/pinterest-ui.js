(() => { let done=false; async function api(path,opts={}){const t=localStorage.getItem('mlai_token')||'';opts.headers={...(opts.headers||{}),'Content-Type':'application/json',...(t?{Authorization:'Bearer '+t}:{})};const r=await fetch('/api/v1'+path,opts);if(!r.ok)throw new Error(await r.text());return r.json();} function mount(){const panel=document.getElementById('pinterest');if(!panel||done)return;done=true;const card=document.createElement('div');card.className='card';card.innerHTML='<h2>4. Diagnóstico da API</h2><p class="muted">Testa a autorização atual contra a API do Pinterest e verifica se os boards podem ser lidos.</p><button class="btn" id="pinDiag">Testar sincronização</button><div id="pinDiagOut" class="muted" style="margin-top:10px"></div>';panel.querySelector('.settings-grid')?.appendChild(card);document.getElementById('pinDiag').onclick=async()=>{const out=document.getElementById('pinDiagOut');out.textContent='Testando...';try{const d=await api('/marketplace/pinterest/diagnostic');out.innerHTML=d.apiReachable?`<span class="status ok-bg">● API acessível · ${d.boardsCount||0} boards encontrados</span>`:`<span class="status warn">○ Não acessível</span><pre>${JSON.stringify(d,null,2)}</pre>`}catch(e){out.textContent='Não foi possível testar agora.'}};} new MutationObserver(mount).observe(document.documentElement,{childList:true,subtree:true});setTimeout(mount,900);})();
