const API='/api/v1';let token=localStorage.getItem('mlai_token');async function api(path,opts={}){opts.headers={...(opts.headers||{}),'Content-Type':'application/json',...(token?{Authorization:'Bearer '+token}:{})};const r=await fetch(API+path,opts);if(!r.ok)throw new Error((await r.text())||r.statusText);return r.json()}function login(){document.getElementById('app').innerHTML=`<div class="wrap"><div class="card" style="max-width:440px;margin:12vh auto"><h1>ML Affiliate AI</h1><p class="muted">Seu painel automático de afiliados.</p><label>E-mail</label><input id="email" class="input" type="email"><label>Senha</label><input id="password" class="input" type="password"><div id="err" class="error"></div><button class="btn" onclick="doLogin()">Entrar</button></div></div>`}async function doLogin(){try{const d=await api('/auth/login',{method:'POST',body:JSON.stringify({email:email.value,password:password.value})});token=d.token;localStorage.setItem('mlai_token',token);dashboard()}catch(e){document.getElementById('err').textContent='E-mail ou senha inválidos.'}}async function dashboard(){try{const me=await api('/auth/me');const st=await api('/marketplace/mercadolivre/status');document.getElementById('app').innerHTML=`<div class="wrap"><div class="top"><div><h1>ML Affiliate AI</h1><div class="muted">Olá, ${me.name}</div></div><button class="btn alt" onclick="logout()">Sair</button></div><div class="grid"><div class="card"><div class="muted">Mercado Livre</div><h2>${st.connected?'Conectado':'Não conectado'}</h2><p>${st.configured?'Integração configurada':'Falta configurar as credenciais da aplicação'}</p>${st.connected?'':'<button class="btn" onclick="connectML()">Conectar Mercado Livre</button>'}</div><div class="card"><div class="muted">Robô</div><h2>Pronto para operar</h2><p>Busca, análise, score e automações.</p></div><div class="card"><div class="muted">Regra financeira</div><h2>100% protegido</h2><p>Nenhum gasto sem sua aprovação.</p></div></div><div class="card" style="margin-top:16px"><h2>Próximas ações</h2><p>1. Conectar Mercado Livre</p><p>2. Definir categorias e limites</p><p>3. Ativar Product Hunter e geração de conteúdo</p><p class="muted">As funções de afiliado que não forem expostas oficialmente pela API ficam bloqueadas como NOT_SUPPORTED.</p></div></div>`}catch(e){token=null;localStorage.removeItem('mlai_token');login()}}function connectML(){window.location.href=API+'/marketplace/mercadolivre/connect'}async function logout(){try{await api('/auth/logout',{method:'POST'})}catch{}localStorage.removeItem('mlai_token');token=null;login()}if(token)dashboard();else login();