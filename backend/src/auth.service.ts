import {Injectable,UnauthorizedException} from '@nestjs/common'; import {PrismaService} from './prisma.service'; import * as crypto from 'crypto'; @Injectable() export class AuthService{constructor(private db:PrismaService){} hash(s:string){return crypto.createHash('sha256').update(s).digest('hex')} async ensureAdmin(){const email=process.env.ADMIN_EMAIL;if(!email)return;const password=process.env.ADMIN_PASSWORD||'change-me';const e=await this.db.user.findUnique({where:{email}});if(!e)await this.db.user.create({data:{name:'Administrador',email,passwordHash:this.hash(password)}})} async login(email:string,password:string){const u=await this.db.user.findUnique({where:{email}});if(!u||u.passwordHash!==this.hash(password))throw new UnauthorizedException('INVALID_CREDENTIALS');const token=crypto.randomBytes(32).toString('hex');await this.db.session.create({data:{tokenHash:this.hash(token),userId:u.id,expiresAt:new Date(Date.now()+30*86400000)}});return {token,user:{id:u.id,name:u.name,email:u.email}}} async userFromToken(token?:string){if(!token)return null;const s=await this.db.session.findUnique({where:{tokenHash:this.hash(token)},include:{user:true}});if(!s||s.expiresAt<new Date())return null;return s.user} async logout(token:string){if(token)await this.db.session.deleteMany({where:{tokenHash:this.hash(token)}});return {ok:true}}}